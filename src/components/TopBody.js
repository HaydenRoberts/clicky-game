import React from "react";

const TopBody = props => (
  <div className="top-body row">
    <div className="instructions">Click on an image to earn points, <br/>but don't click on any more than once!</div>
  </div>
);

export default TopBody;